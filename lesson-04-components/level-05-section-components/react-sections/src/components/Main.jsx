export function Main() {
  return (
    <main>
      <p>
        {" "}
        React components are used by making a file inside the components folder
        of a react and exporting a function that returns the component you want.
      </p>
    </main>
  );
}
